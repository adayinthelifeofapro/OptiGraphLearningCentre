@page "/"
@inject ILearningService LearningService
@inject ISettingsService SettingsService
@inject AppState AppState
@inject NavigationManager Navigation

<PageTitle>Optimizely Graph Learning Centre</PageTitle>

<div class="max-w-6xl mx-auto space-y-8">
    <!-- Hero Section -->
    <div class="bg-gradient-to-r from-opti-blue to-blue-600 rounded-2xl p-8 text-white">
        <div class="flex items-start justify-between">
            <div>
                <h1 class="text-3xl font-bold mb-2">Welcome to the Optimizely Graph Learning Centre</h1>
                <p class="text-blue-100 text-lg max-w-2xl">
                    Master GraphQL queries with Optimizely Graph through interactive lessons,
                    a visual query builder, and hands-on examples.
                </p>
            </div>
            <div class="hidden lg:block">
                <svg class="w-24 h-24 text-white/20" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M13 10V3L4 14h7v7l9-11h-7z"/>
                </svg>
            </div>
        </div>

        <div class="flex gap-4 mt-6">
            @if (AppState.ConnectionStatus != ConnectionStatus.Connected)
            {
                <a href="/settings" class="btn bg-white text-opti-blue hover:bg-blue-50">
                    Configure Connection
                </a>
            }
            <a href="/learn" class="btn bg-blue-500 text-white hover:bg-blue-400 border border-blue-400">
                Start Learning
            </a>
            <a href="/query-builder" class="btn bg-transparent text-white border border-white/30 hover:bg-white/10">
                Query Builder
            </a>
        </div>
    </div>

    <!-- Quick Stats -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
        <div class="card">
            <div class="card-body flex items-center gap-4">
                <div class="w-12 h-12 bg-blue-100 dark:bg-blue-900/50 rounded-lg flex items-center justify-center">
                    <svg class="w-6 h-6 text-opti-blue dark:text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"/>
                    </svg>
                </div>
                <div>
                    <p class="text-2xl font-bold text-gray-900 dark:text-gray-100">@_modules.Count</p>
                    <p class="text-sm text-gray-500 dark:text-gray-400">Learning Modules</p>
                </div>
            </div>
        </div>
        <div class="card">
            <div class="card-body flex items-center gap-4">
                <div class="w-12 h-12 bg-green-100 dark:bg-green-900/50 rounded-lg flex items-center justify-center">
                    <svg class="w-6 h-6 text-green-600 dark:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                    </svg>
                </div>
                <div>
                    <p class="text-2xl font-bold text-gray-900 dark:text-gray-100">@_totalLessons</p>
                    <p class="text-sm text-gray-500 dark:text-gray-400">Total Lessons</p>
                </div>
            </div>
        </div>
        <div class="card">
            <div class="card-body flex items-center gap-4">
                <div class="w-12 h-12 bg-purple-100 dark:bg-purple-900/50 rounded-lg flex items-center justify-center">
                    <svg class="w-6 h-6 text-purple-600 dark:text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"/>
                    </svg>
                </div>
                <div>
                    <p class="text-2xl font-bold text-gray-900 dark:text-gray-100">@_totalExamples</p>
                    <p class="text-sm text-gray-500 dark:text-gray-400">Code Examples</p>
                </div>
            </div>
        </div>
        <div class="card">
            <div class="card-body flex items-center gap-4">
                <div class="w-12 h-12 bg-yellow-100 dark:bg-yellow-900/50 rounded-lg flex items-center justify-center">
                    <svg class="w-6 h-6 text-yellow-600 dark:text-yellow-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
                    </svg>
                </div>
                <div>
                    <p class="text-2xl font-bold text-gray-900 dark:text-gray-100">@_completedLessons</p>
                    <p class="text-sm text-gray-500 dark:text-gray-400">Completed</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Learning Modules Grid -->
    <div>
        <div class="flex items-center justify-between mb-4">
            <h2 class="text-xl font-semibold text-gray-900 dark:text-gray-100">Learning Modules</h2>
            <a href="/learn" class="text-opti-blue dark:text-blue-400 hover:text-blue-700 dark:hover:text-blue-300 text-sm font-medium">
                View all →
            </a>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
            @foreach (var module in _modules.Take(6))
            {
                <a href="@($"/learn/{module.Id}")" class="card hover:shadow-lg transition-shadow group">
                    <div class="card-body">
                        <div class="flex items-start gap-4">
                            <div class="w-10 h-10 bg-opti-blue/10 dark:bg-blue-900/50 rounded-lg flex items-center justify-center flex-shrink-0 group-hover:bg-opti-blue/20 dark:group-hover:bg-blue-800/50 transition-colors">
                                <svg class="w-5 h-5 text-opti-blue dark:text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"/>
                                </svg>
                            </div>
                            <div class="flex-1 min-w-0">
                                <h3 class="font-semibold text-gray-900 dark:text-gray-100 group-hover:text-opti-blue dark:group-hover:text-blue-400 transition-colors">
                                    @module.Title
                                </h3>
                                <p class="text-sm text-gray-500 dark:text-gray-400 mt-1 line-clamp-2">
                                    @module.Description
                                </p>
                                <div class="flex items-center gap-3 mt-3">
                                    <Badge Variant="@GetDifficultyBadge(module.Difficulty)">
                                        @module.Difficulty.ToString()
                                    </Badge>
                                    <span class="text-xs text-gray-400 dark:text-gray-500">
                                        @module.Lessons.Count lessons
                                    </span>
                                </div>
                            </div>
                        </div>
                    </div>
                </a>
            }
        </div>
    </div>

    <!-- Quick Actions -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <Card Title="Visual Query Builder">
            <ChildContent>
            <p class="text-gray-600 dark:text-gray-400 mb-4">
                Build GraphQL queries visually without writing code. Select content types,
                add filters, configure sorting, and see the generated query in real-time.
            </p>
            <a href="/query-builder" class="btn-primary inline-flex items-center gap-2">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"/>
                </svg>
                Open Query Builder
            </a>
            </ChildContent>
        </Card>

        <Card Title="GraphQL Playground">
            <ChildContent>
            <p class="text-gray-600 dark:text-gray-400 mb-4">
                Write and execute raw GraphQL queries directly. Perfect for testing complex
                queries, exploring the schema, and experimenting with advanced features.
            </p>
            <a href="/playground" class="btn-primary inline-flex items-center gap-2">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"/>
                </svg>
                Open Playground
            </a>
            </ChildContent>
        </Card>
    </div>
</div>

@code {
    private List<LearningModule> _modules = new();
    private int _totalLessons;
    private int _totalExamples;
    private int _completedLessons;

    protected override async Task OnInitializedAsync()
    {
        _modules = await LearningService.GetModulesAsync();
        _totalLessons = _modules.Sum(m => m.Lessons.Count);
        _totalExamples = _modules.Sum(m => m.Lessons.Sum(l => l.Examples.Count));

        var progress = await LearningService.GetUserProgressAsync();
        _completedLessons = progress.CompletedLessons.Count(p => p.Value.IsCompleted);

        // Check connection status
        var settings = await SettingsService.GetSettingsAsync();
        if (!string.IsNullOrEmpty(settings.SingleKey) || !string.IsNullOrEmpty(settings.AppKey))
        {
            AppState.ConnectionStatus = ConnectionStatus.Unknown;
        }
    }

    private BadgeVariant GetDifficultyBadge(ModuleDifficulty difficulty) => difficulty switch
    {
        ModuleDifficulty.Beginner => BadgeVariant.Success,
        ModuleDifficulty.Intermediate => BadgeVariant.Warning,
        ModuleDifficulty.Advanced => BadgeVariant.Danger,
        _ => BadgeVariant.Default
    };
}
